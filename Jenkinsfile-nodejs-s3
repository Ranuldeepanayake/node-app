#!/usr/bin/env groovy

pipeline {
  agent { 
	  label 'jenkins-master'
  }
	
  stages {

    stage('Build docker images') {
      steps {
        //sh "sed -i 's/DOCKERFILE_SOURCE_CODE_PATH/$WORKSPACE/g' $WORKSPACE/docker/dockerfile-nodejs-s3"
	      //sh "docker build -f $WORKSPACE/docker/dockerfile-nodejs-s3 -t ranuldeepanayake/private:nodejs-s3 /"
        //sh "docker push ranuldeepanayake/private:nodejs-s3" 
        sh "echo 'hello world'"
      }
    }

    stage('Deploy on Kubernetes') {
      steps {
        // sh 'kubectl apply -f /home/ranul/workspace/nodejs-s3/kubernetes/nodejs-s3-deployment.yml'
        // sh 'kubectl apply -f /home/ranul/workspace/nodejs-s3/kubernetes/nodejs-s3-service.yml'

        withKubeconfig([credentialsId: 'Jenkins-Plugin-Kubernetes', serverUrl: 'https://192.168.56.128:6443']) {
          sh 'kubectl get deployments'
        }
      }
    }

    // stage('Code security analysis'){
    //   environment {
    //     scannerHome = tool 'SonarQube'
    //   }
    //   steps {
    //     script {
    //       withSonarQubeEnv('SonarQube') {
    //         sh "${scannerHome}/bin/sonar-scanner \
    //             -Dsonar.projectKey=node-app \
    //             -Dsonar.host.url=http://192.168.56.126:9000 \
    //             -Dsonar.token=sqa_9ca3249747b23cc2fac39510d39064b6e830788a \
    //             -Dsonar.sources=. "
    //       }
    //     }
    //   }
    // }
  }
}