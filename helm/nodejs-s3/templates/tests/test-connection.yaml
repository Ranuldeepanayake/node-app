apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodejs-s3.fullname" . }}-test-connection"
  labels:
    {{- include "nodejs-s3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodejs-s3.fullname" . }}:{{ .Values.service1.port }}']
  restartPolicy: Never
