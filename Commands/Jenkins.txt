https://<access token>@github.com/<userName>/<repository>.git

#Git repositiory link with credentials
https://<token>@github.com/Ranuldeepanayake/node-app.git

#Webhook relay provider.
https://webhookrelay.com/

#Webhook relay key
relay login -k <> -s <>

#Webhook relay command
relay forward --bucket gitlab-jenkins http://localhost:8080/github-webhook/

#Multibranch webhook URL.
relay forward --bucket gitlab-jenkins http://localhost:8080/multibranch-webhook-trigger/invoke?token=my-token

